<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <%- include('../partials/header') %>

    <title>Budget Homepage</title>
</head>
<body>
        <div class="row row-cols-1 row-cols-md-3 g-4">
          <% budgets.forEach(b => { %>
            
             <div class="col">
             <div class="card">
                <div class="card-body">
                  <h5 class="card-title"><%= b.month %></h5>
                  <h6 class="card-subtitle mb-2 text-muted">Budget Amount: $<%= b.amount %></h6>
                  <a href="/budgets/<%= b._id %>/edit " class="card-link">Edit</a>
                  <a href="/budgets/<%= b._id %> " class="card-link">Details</a>
                </br></br>
                  <form action=<%= `/budgets/${b._id}/?_method=DELETE` %> method="POST" id="deleteBudget">
                    <button type="submit" class="card-footer bg-transparent border-success">DELETE</button>
                </form>
                </div>
              </div>
            </div>            
          <% }) %>
        </div>
</body>
</html>